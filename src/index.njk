---
layout: home.njk
title: Home Page
sitemap:
  changefreq: weekly
  priority: 1.0
---

<section class="home-hero">
  <h1 class="text-12 text-weight-550">Isn't it time to bring your vision to life?</h1>
  <p class="text-8 text-weight-400 text-charcoal-light">We help founders, nonprofits, and changemakers amplify their impact and achieve results.</p>
  <a href="/about" class="button">About us</a>
</section>

<section class="projects container-xl">
    <a href="/projects/accountable-tech" class="project project-ej will-animate">
      <div class="project-details">
        <h4 class="text-9 m-0 mb-1">Encode Justice</h4>
        <p class="text-7 m-0 text-weight-400">Empowering the youth to safeguard the future of AI.</p>
      </div>
    </a>
    <a href="/projects/accountable-tech" class="project project-fm will-animate">
      <div class="project-details">
        <h4 class="text-9 m-0 mb-1">Forward Majority</h4>
        <p class="text-7 m-0 text-weight-400">A fresh look for a stronger democracy.</p>
      </div>
    </a>
    <a href="/projects/accountable-tech" class="project project-at will-animate">
      <div class="project-details">
        <h4 class="text-9 m-0 mb-1">Accountable Tech</h4>
        <p class="text-7 m-0 text-weight-400">A bold new look for a more accountable future.</p>
      </div>
    </a>
    <a href="/projects/accountable-tech" class="project project-ndf c-animate-reveal">
      <div class="project-details">
        <h4 class="text-9 m-0 mb-1">No Deepfakes for Democracy</h4>
        <p class="text-7 m-0 text-weight-400">Fighting deepfakes. Protecting truth.</p>
      </div>
    </a>
    <a href="/projects" class="text-4 link-chevron text-weight-400 pt-6">View more projects</a>
</section>

<section class="cta">
  <div class="cta-container">
    <h4 class="text-10 text-weight-600">Our actions today shape tomorrow. Let's get started.</h4>
    <a href="/contact" class="button">Contact us</a>
  </div>
</section>

<div id="load-screen" class="load-screen">
  <div class="progress-bar"></div>
  <div class="load-text">Loading...</div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', () => {
      // Check if the current path is the home screen
      if (window.location.pathname === '/') {
          const loadScreen = document.getElementById('load-screen');
          const progressBar = document.querySelector('.progress-bar');
          const loadText = document.querySelector('.load-text');

          // Function to generate random 8-letter uppercase strings
          const generateRandomText = () => {
              const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ';
              let text = '';
              for (let i = 0; i < 8; i++) {
                  text += chars[Math.floor(Math.random() * chars.length)];
              }
              return "[" + text + "]";
          };

          // Update the load text with random strings
          const interval = setInterval(() => {
              loadText.textContent = generateRandomText();
          }, 100); // Update every 100ms

          // When the progress bar animation ends
          progressBar.addEventListener('animationend', () => {
              clearInterval(interval); // Stop random text updates

              // Show "LOOPDASH" 0.25 seconds before fade-out
              setTimeout(() => {
                  loadText.textContent = '[LOOPDASH]';
              }, 400); // 1s - 0.25s = 0.75s

              // Fade out the load screen
              setTimeout(() => {
                  loadScreen.style.opacity = '0'; // Fade out
                  loadScreen.style.pointerEvents = 'none';

                  // Remove load screen after fade-out
                  setTimeout(() => {
                      loadScreen.remove();
                  }, 500); // Match the CSS transition duration
              }, 500); // Total animation duration
          });
      }
  });
</script>
