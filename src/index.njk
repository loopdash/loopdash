---
layout: home.njk
meta: 
  title: Loopdash 
  description: Loopdash partners with nonprofits and businesses to deliver expert web design, development, SEO, and hosting solutions that drive results and simplify growth.
pagination:
  data: collections.projects
  size: 10
  alias: projects
sitemap:
  changefreq: weekly
  priority: 1.0
---

<section class="hero">
  <div class="video-bkg"></div>
  <video autoplay muted loop defaultMuted playsinline preload="auto" class="video" oncontextmenu="return false;">
    <source src="/video/forest-compressed.mp4" type="video/mp4">
    <source src="video/forest-compressed.webm" type="video/webm">
  </video>
  <div class="text-set text-set-home pb-24">
    <h1 class="text-11 text-weight-600 text-gradient-white">Bringing Simplicity Back to Web Development.</h1>
    <p class="text-7 text-weight-400">We help founders, nonprofits, and changemakers amplify their impact and achieve results.</p>
    <a href="/about" class="button button-secondary plausible-event-name=Home+About+Button+Click">About us</a>
  </div>
</section>

<section class="projects container-xl mt-16">
  {% for project in projects | sortByDate %}
    {% if project.data.project.featured %}
      <a href="{{project.data.page.url}}" class="project project-{{project.data.project.class}} plausible-event-name=Home+Project+Card+Click+{{project.data.project.class | replace('-', '+')}}">
        <div class="project-details">
          <h4 class="text-8 m-0">{{ project.data.project.name }}</h4>
          <p class="text-4 m-0 text-weight-400">{{ project.data.project.tagline }}</p>
        </div>
      </a>
    {% endif %}
  {% endfor %}
</section>

<section class="cta">
  <div class="cta-container">
    <h4 class="text-10 text-weight-600">Our actions today shape tomorrow. Let's get started.</h4>
    <a href="/contact" class="shake button button-primary plausible-event-name=Home+CTA+Button+Click">Contact us</a>
  </div>
</section>

