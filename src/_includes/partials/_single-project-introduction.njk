<div class="introduction introduction-{{project.class}}">
  <div class="flex justify-between mx-auto py-24 container-lg">
    <div class="col-7">
      <p class="text-6 text-weight-300 text-white">{{ project.description | safe}}</p>
    </div>
    <div class="col-3 details">
      <h5 class="text-3 mb-0 text-weight-600 text-uppercase">Industry</h5>
      <ul class="text-3">
        <li>{{project.industry}}</li>
      </ul>

      {% if project.services %}
        <h5 class="text-3 mb-0 text-weight-600 text-uppercase">Services</h5>
          <ul class="text-3">
          {% for service in project.services %}
            <li>{{ service.name }}</li>
          {% endfor %}
        </ul>
      {% endif %}

      {% if project.collaborators %}
        <h5 class="text-3 mb-0 text-weight-600 text-uppercase">Collaborators</h5>
          <ul class="text-3">
          {% for collaborator in project.collaborators %}
            <li>{{ collaborator.name }}</li>
          {% endfor %}
        </ul>
      {% endif %}

      {% if project.press %}
        <h5 class="text-3 mb-0 text-weight-600 text-uppercase">Press</h5>
          <ul class="text-3">
          {% for item in project.press %}
            <li><a href="{{ item.link }}" target="_blank" class="press-link">{{ item.name }} <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 12 12" width="12" height="12" color="#ffffff"><path d="M4.7 10c-.2 0-.4-.1-.5-.2-.3-.3-.3-.8 0-1.1L6.9 6 4.2 3.3c-.3-.3-.3-.8 0-1.1.3-.3.8-.3 1.1 0l3.3 3.2c.3.3.3.8 0 1.1L5.3 9.7c-.2.2-.4.3-.6.3Z"></path></svg></a></li>
          {% endfor %}
        </ul>
      {% endif %}

      {% if project.website %}
        <h5 class="text-3 mb-0 text-weight-600 text-uppercase">Website</h5>
          <ul class="text-3">
            <li>{{ project.website }}</li>
        </ul>
      {% endif %}
    </div>
  </div>
</div>